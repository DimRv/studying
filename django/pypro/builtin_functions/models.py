from django.db import models

# Create your models here.


"""class BuiltInFunctions(models.Model):
    name = models.CharField("Функция", max_length=20, unique=True)
    syntax = models.TextField("Синтаксис")
    short_description = models.CharField("Короткое Описание", max_length=200)
    description = models."""

